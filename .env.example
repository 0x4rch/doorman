# Copy to .env and fill in. Nothing else is needed.
TYPESAFE_API_KEY=

# Optional
JEV_MODEL=jev-latest          # or pin a version, e.g. jev-1.13.0
DOORMAN_MODE=auto             # auto: live when a key is set, mock otherwise. Or force: live | mock
PORT=3000
